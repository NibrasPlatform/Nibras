export enum VideoProvider {
  YouTube = 'youtube',
  Vimeo = 'vimeo',
  Custom = 'custom',
}

export enum VideoEventType {
  VideoStarted = 'VIDEO_STARTED',
  VideoProgressUpdated = 'VIDEO_PROGRESS_UPDATED',
  VideoCompleted = 'VIDEO_COMPLETED',
}
