FROM python:3.11-slim

WORKDIR /app

COPY apps/tutor/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY apps/tutor/ .

ENV PORT=5000
EXPOSE 5000

CMD gunicorn --bind 0.0.0.0:${PORT:-5000} --timeout 120 --graceful-timeout 30 --workers 1 --threads 4 app:app
